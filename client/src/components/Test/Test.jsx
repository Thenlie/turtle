import React from 'react';
import { useMutation } from '@apollo/client';


const Test = () => {
    return (
        <>
            <button>Login</button>
            <button>Logout</button>
        </>
    )
};

export default Test;