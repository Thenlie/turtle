import { Info, Scores, Stats } from "../components/Profile";

const Profile = () => {
    return (
        <main>
            <Info />
            <Scores />
            <Stats />
        </main>
    );
};

export default Profile;